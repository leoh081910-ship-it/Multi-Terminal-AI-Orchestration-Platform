---
phase: quick-260513-0mv-lint
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/components/LogViewer.tsx
  - web/src/pages/AgentWorkbenchPage.tsx
  - web/src/pages/TaskDetailPage.tsx
autonomous: true
requirements:
  - QUICK-LINT-01
must_haves:
  truths:
    - "Frontend lint command completes with zero React compiler/hook warnings for the reported files."
    - "Frontend production build still succeeds after lint fixes."
    - "Go backend test suite remains green after frontend-only changes."
    - "Executor can summarize the current development plan from ROADMAP/STATE after fixes."
  artifacts:
    - path: "web/src/components/LogViewer.tsx"
      provides: "Log viewer without set-state-in-effect lint violation"
      contains: "useState"
    - path: "web/src/pages/AgentWorkbenchPage.tsx"
      provides: "Agent workbench render path without Date.now purity violation"
      contains: "AgentWorkbenchPage"
    - path: "web/src/pages/TaskDetailPage.tsx"
      provides: "Task detail effects without unused variables or missing dependency warnings"
      contains: "fetchTask"
  key_links:
    - from: "web/src/pages/TaskDetailPage.tsx"
      to: "task detail data loading"
      via: "useCallback/useEffect dependency-safe fetchTask"
      pattern: "useCallback|useEffect"
    - from: "web/src/pages/AgentWorkbenchPage.tsx"
      to: "rendered timestamp/age values"
      via: "stable state/effect timestamp source instead of Date.now during render"
      pattern: "Date\.now"
---

<objective>
Fix the currently reported frontend lint issues while preserving existing build/test health.

Purpose: React compiler lint must be clean before continuing planned development, and the executor should also understand the active roadmap/state well enough to summarize the development plan after the fix.
Output: Targeted frontend lint fixes in the reported files, verified by lint/build/backend tests, plus a short development-plan summary in the execution summary.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md

<known_lint_failures>
`npm --prefix web run lint` currently reports these frontend issues to fix:
- `web/src/components/LogViewer.tsx:37` — `react-hooks/set-state-in-effect`
- `web/src/pages/AgentWorkbenchPage.tsx:73` — `react-hooks/purity` for `Date.now` during render
- `web/src/pages/TaskDetailPage.tsx:69` — unused `_execution`
- `web/src/pages/TaskDetailPage.tsx:58` and `:67` — missing `fetchTask` dependency warnings
</known_lint_failures>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix React lint violations in reported frontend files</name>
  <files>web/src/components/LogViewer.tsx, web/src/pages/AgentWorkbenchPage.tsx, web/src/pages/TaskDetailPage.tsx</files>
  <action>
    Make the smallest safe edits for the reported lint issues only. Avoid eslint-disable comments.
  </action>
  <verify>
    <automated>npm --prefix web run lint</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Run full requested verification commands</name>
  <verify>
    <automated>npm --prefix web run build</automated>
    <automated>go test ./...</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Summarize development plan from planning state</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md, .planning/quick/260513-0mv-lint/260513-0mv-SUMMARY.md</files>
  <action>
    Read `.planning/ROADMAP.md` and `.planning/STATE.md` enough to summarize the current development plan after the lint fix.
  </action>
</task>

</tasks>

<verification>
1. `npm --prefix web run lint`
2. `npm --prefix web run build`
3. `go test ./...`
</verification>

<output>
After completion, create `.planning/quick/260513-0mv-lint/260513-0mv-SUMMARY.md`.
</output>
