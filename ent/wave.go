// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/mCP-DevOS/ai-orchestration-platform/ent/wave"
)

// Wave is the model entity for the Wave schema.
type Wave struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// ProjectID holds the value of the "project_id" field.
	ProjectID string `json:"project_id,omitempty"`
	// DispatchRef holds the value of the "dispatch_ref" field.
	DispatchRef string `json:"dispatch_ref,omitempty"`
	// Wave holds the value of the "wave" field.
	Wave int `json:"wave,omitempty"`
	// SealedAt holds the value of the "sealed_at" field.
	SealedAt time.Time `json:"sealed_at,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt    time.Time `json:"created_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Wave) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case wave.FieldID, wave.FieldWave:
			values[i] = new(sql.NullInt64)
		case wave.FieldProjectID, wave.FieldDispatchRef:
			values[i] = new(sql.NullString)
		case wave.FieldSealedAt, wave.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Wave fields.
func (_m *Wave) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case wave.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case wave.FieldProjectID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field project_id", values[i])
			} else if value.Valid {
				_m.ProjectID = value.String
			}
		case wave.FieldDispatchRef:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field dispatch_ref", values[i])
			} else if value.Valid {
				_m.DispatchRef = value.String
			}
		case wave.FieldWave:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field wave", values[i])
			} else if value.Valid {
				_m.Wave = int(value.Int64)
			}
		case wave.FieldSealedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field sealed_at", values[i])
			} else if value.Valid {
				_m.SealedAt = value.Time
			}
		case wave.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Wave.
// This includes values selected through modifiers, order, etc.
func (_m *Wave) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this Wave.
// Note that you need to call Wave.Unwrap() before calling this method if this Wave
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *Wave) Update() *WaveUpdateOne {
	return NewWaveClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the Wave entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *Wave) Unwrap() *Wave {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: Wave is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *Wave) String() string {
	var builder strings.Builder
	builder.WriteString("Wave(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("project_id=")
	builder.WriteString(_m.ProjectID)
	builder.WriteString(", ")
	builder.WriteString("dispatch_ref=")
	builder.WriteString(_m.DispatchRef)
	builder.WriteString(", ")
	builder.WriteString("wave=")
	builder.WriteString(fmt.Sprintf("%v", _m.Wave))
	builder.WriteString(", ")
	builder.WriteString("sealed_at=")
	builder.WriteString(_m.SealedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// Waves is a parsable slice of Wave.
type Waves []*Wave
